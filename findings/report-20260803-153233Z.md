# gtm-sdk#445 sandbox findings — 20260803-153233Z

Harness: `scripts/sandbox-test.sh` @ 5f90900
Options: FLAKE_REPRO=0, FLOXHUB_TEST_PKG=elvis/conductor-workspace-floxhub-01, TEST_AUTH_PLUMBING=0, TEST_FLOXHUB_PROVISION=1

| Stage | Result | Detail |
|---|---|---|
| 0 fingerprint | **PASS** | recorded (see report body) |
| 1 flox bootstrap | **PASS** | flox installed and responding (0s) |
| 2 H1 prebuilt activate | **PASS** | all 5 tools resolve under .flox/run and execute (0s) |
| 3a hello build | **PASS** | trivial no-network build + run OK (2s) |
| 3b bd repackage build | **PASS** | built + `bd version` → bd version 1.1.2 (20e493e56: HEAD@20e493e569c9) (3s) |
| 3c bd flag surface | **PASS** | all conductor-workspace-setup.sh flags present on v1.1.2 |
| 4 FloxHub fetch | **SKIP** | 'flox show elvis/conductor-workspace-floxhub-01' failed unauthenticated — expected: a bare 'flox publish' is private-by-default (issue #16 §1), not ambiguous. See the flox search output above for whether the catalog is discoverable at all unauthenticated. Re-run with TEST_AUTH_PLUMBING=1 to exercise the authenticated Phase D' consumption path (Stage 6) instead. |
| 5 H2 flake repro | **SKIP** | opt-in: re-run with FLAKE_REPRO=1 (costs a doomed Go build, ~minutes) |
| 6 auth plumbing | **SKIP** | opt-in: re-run with TEST_AUTH_PLUMBING=1 FLOXHUB_TOKEN=<token> (permanently authenticates this sandbox — never run on the one testing Stage 4) |
| 7 floxhub provision MVP | **PASS** | all 7 tools (catalog tools + real bd/roborev packages) resolve under .flox/run and execute (4s) |

## Stage 0 — environment fingerprint

**kernel/arch** (`uname -srm`, exit 0):

```
Linux 6.12.76 x86_64
```

**os-release** (`sh -c head -2 /etc/os-release 2>/dev/null || echo "no /etc/os-release"`, exit 0):

```
NAME="Amazon Linux"
VERSION="2023"
```

**nix.conf** (`sh -c cat /etc/nix/nix.conf 2>/dev/null || echo "no /etc/nix/nix.conf"`, exit 0):

```

build-users-group =
include flox.conf
```

**nixbld build users** (`sh -c getent group nixbld || echo "no nixbld group"`, exit 0):

```
no nixbld group
```

**/dev/fd** (`sh -c ls -ld /dev/fd 2>/dev/null || echo "MISSING (gtm-sdk#279 territory)"`, exit 0):

```
lrwxrwxrwx 1 root root 13 Aug  3 15:05 /dev/fd -> /proc/self/fd
```

**/homeless-shelter** (`sh -c ls -ld /homeless-shelter 2>/dev/null || echo "absent (good)"`, exit 0):

```
absent (good)
```

**nix-daemon socket** (`sh -c ls -l /nix/var/nix/daemon-socket/socket 2>/dev/null || echo "absent"`, exit 0):

```
srw-rw-rw- 1 root root 0 Aug  3 15:05 /nix/var/nix/daemon-socket/socket
```

**preexisting flox/nix** (`sh -c command -v flox nix || echo "neither on PATH"`, exit 0):

```
/usr/bin/flox
/usr/bin/nix
```


## Stage 1 — flox bootstrap

flox already present: /usr/bin/flox
**flox version** (`flox --version`, exit 0):

```
1.14.0
```

nix-daemon socket already present.
**effective nix.conf after install** (`sh -c cat /etc/nix/nix.conf 2>/dev/null || echo "no /etc/nix/nix.conf"`, exit 0):

```

build-users-group =
include flox.conf
```


## Stage 2 — H1: prebuilt-only manifest materializes atomically

Activation succeeded in 0s (includes in-sandbox lock/resolution: no manifest.lock is committed).
FLOX_BIN: `/home/vercel-sandbox/workspace/envs/prebuilt/.flox/run/x86_64-linux.prebuilt-run/bin`
- `uv`: OK — uv 0.11.26 (x86_64-unknown-linux-gnu)
- `dolt`: OK — dolt version 2.1.10
- `infisical`: OK — infisical version 0.41.90
- `gh`: OK — gh version 2.96.0 (nixpkgs)
- `git`: OK — git version 2.54.0

## Stage 3 — H3: flox manifest builds (Option-A repackage shape)


## Stage 4 — H4: FloxHub catalog fetch (the Phase A fork in the road)

**flox auth status (expect logged-out on a fresh sandbox)** (`sh -c flox auth status 2>&1 || true`, exit 0):

```
! You are not currently logged in to FloxHub.
```

**flox search elvis/conductor-workspace-floxhub-01 (expect no results unauthenticated)** (`flox search elvis/conductor-workspace-floxhub-01`, exit 1):

```
✘ ERROR: No packages matched this search term: 'elvis/conductor-workspace-floxhub-01'
```

```
✘ ERROR: no packages matched this pkg-path: 'elvis/conductor-workspace-floxhub-01'
```

## Stage 5 — H2: flake source-build failure repro (control)


## Stage 6 — Phase D' prototype: authenticated FloxHub consumption


## Stage 7 — Phase D MVP: combined FloxHub provisioning (bd + roborev + catalog tools)

floxhub-provision.sh succeeded in 4s; FLOX_BIN: `/home/vercel-sandbox/workspace/envs/floxhub-provision/.flox/run/x86_64-linux.floxhub-provision-run/bin`.
- `uv`: OK — uv 0.11.26 (x86_64-unknown-linux-gnu)
- `dolt`: OK — dolt version 2.1.9
- `infisical`: OK — infisical version 0.41.90
- `gh`: OK — gh version 2.96.0 (nixpkgs)
- `git`: OK — git version 2.54.0
- `bd`: OK — bd version 1.1.2 (20e493e56: HEAD@20e493e569c9)
- `roborev`: OK — roborev v0.63.0
